name: second

on:
  workflow_run:
    workflows:
      - learn-github-actions
    types:
      - completed

jobs:
  SecondAction:
    runs-on: ubuntu-latest
    
    steps:
      - run: |
          echo "GOOD"